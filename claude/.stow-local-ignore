# Do not symlink reference markdown docs to ~/.claude/.
# Keep this an explicit list: a blanket `.*\.md` also matches CLAUDE.md, which
# MUST be symlinked (it's the global agent instructions). A bare pattern only
# "works" on a machine where the CLAUDE.md link predates this file — a fresh
# `stow` would silently skip it.
cheatsheet\.md
claudecode-nvim-vs-tmux\.md
